<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.moskofidi.mychat">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/allo_logo"
        android:label="@string/app_name"
        android:noHistory="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".MessageNotificationService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".signInActivity.RegisterActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signInActivity.SignInActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".signInActivity.RegisterActivity" />
        </activity>
        <activity
            android:name=".chatActivity.LatestMessagesActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".chatActivity.ChatActivity"
            android:theme="@style/AppTheme.Chat">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".chatActivity.LatestMessagesActivity" />
        </activity>
        <activity android:name=".chatActivity.NewMessageActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".chatActivity.LatestMessagesActivity" />
        </activity>
    </application>

</manifest>